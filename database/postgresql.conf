# PostgreSQL configuration with intentional security flaws

# Connection settings
listen_addresses = '*'  # Intentional security flaw: listen on all interfaces
port = 5432
max_connections = 100

# Memory settings
shared_buffers = 128MB
effective_cache_size = 4GB
maintenance_work_mem = 64MB
checkpoint_completion_target = 0.9

# Logging settings - Intentional security flaw: excessive logging
log_destination = 'stderr'
logging_collector = on
log_directory = 'log'
log_filename = 'postgresql-%Y-%m-%d_%H%M%S.log'
log_statement = 'all'  # Security flaw: logs all statements including sensitive data
log_min_duration_statement = 0  # Security flaw: logs all query durations

# Security settings with intentional flaws
ssl = off  # Security flaw: SSL disabled
password_encryption = md5  # Security flaw: weak password encryption
log_connections = on
log_disconnections = on

# Intentional security flaw: weak authentication timeout
authentication_timeout = 60s